import { Lexend, Lobster, Red_Hat_Display, Ubuntu } from "next/font/google";

export const lexend = Lexend({ weight: '400', subsets: ['latin'] });

export const lobster = Lobster({ weight: '400', subsets: ['latin'] });

export const ubuntu = Ubuntu({ weight: '400', subsets: ['latin'] });

export const redHat = Red_Hat_Display({weight: '400', subsets: ['latin']});